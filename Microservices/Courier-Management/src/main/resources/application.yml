server:
  port: 8085
spring:
  application:
    name: courier-management
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  datasource:
    url: jdbc:postgresql://localhost:5440/courierdb
    username: postgres
    password: postgres